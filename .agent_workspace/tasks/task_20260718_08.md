# task_20260718_08:T8 cache/system/bigfile 三个扫描器

- 指派给:trae
- 发起者:claude
- 类型:dev
- 创建时间:2026-07-18T22:11:25+08:00

## 任务要求
严格按 docs/superpowers/plans/2026-07-18-ai-cleaner.md 的 Task 8 执行。含沙盒容器 Containers 与 SystemTempDir(getconf DARWIN_USER_CACHE_DIR)两个盲区来源。依赖:T7 已 done(可与 T9 并行)。验收:全量 unittest 绿;./clean-zd scan --category cache --category system --category bigfile 真跑输出合理且安全名单目录不出现。

## 验收标准
(发起者补充:如何算完成)

## 执行记录

- 执行者:trae
- 完成时间:2026-07-18(UTC+8)
- 改动文件:
  - 修改 `cleanzd/scan/cache.py`--替换占位,实现 `_scan_roots(roots, evidence)` + `_darwin_cache_dir()` + `scan()`;来源:`~/Library/Caches/*`、`~/Library/Logs/*`、`~/Library/Application Support/*/Cache{,s}`、`~/Library/Containers/*/Data/Library/Caches`、`getconf DARWIN_USER_CACHE_DIR` 子目录;<1MB 忽略。
  - 修改 `cleanzd/scan/systemitems.py`--废纸篓 / Mail Downloads / iOS 备份,一律 `caution`。
  - 修改 `cleanzd/scan/bigfile.py`--`_scan(roots, thr)` + `scan(threshold_mb, dirs)`;默认 `~/Downloads`+`~/Desktop` 一层,超阈值文件/目录,`caution`,证据含最后修改天数。
  - 追加 `tests/test_scan.py`--`CacheScannerTest`、`BigfileScannerTest` 两个测试类。
- 代码来源:原样落地计划文档 Task 8 Step 3。**一处必要偏离**:`cache._scan_roots` 的 `child.is_dir()` 包了 `try/except OSError`(见下方发现 1),否则真跑崩溃。
- 测试:
  - `python3 -m unittest tests.test_scan -v` -> 6/6 PASS(原 4 + 新增 2)。
  - `python3 -m unittest discover -s tests` -> 46/46 PASS。
- 真跑验证(`./clean-zd scan --category cache --category system --category bigfile`,临时 `CLEAN_ZD_CONFIG_DIR`):
  - 退出码 0,候选输出合理,体积降序(如 Google 5.2GB、pip 1.9GB、JetBrains 872MB、electron 950MB);SystemTempDir 盲区被扫到(com.apple.wallpaper.caches 1.2GB 等)。
- **发现 1(已修,实现层)**:`_scan_roots` 里 `child.is_dir()` 对 SIP 保护的 SystemTempDir 子目录(如 `com.apple.WebKit.GPU.Sandbox`)抛 `PermissionError` 导致崩溃。已加 `try/except OSError: continue` 容错(与 `paths.path_size` 的容错模式一致)。这是最小修复,不改逻辑。
- **发现 2(未修,设计层,已上报 codex)**:真跑输出中,`/var/folders/.../C/`(SystemTempDir)下的 `com.apple.FontRegistry`(49.7MB)、`com.apple.dock.extra`(1.4MB)、`com.apple.Spotlight`(1.3MB)出现在候选中。原因:`safety.SAFETY_GLOBS` 形如 `~/Library/Caches/com.apple.dock*`,`safety_hit` 用 `fnmatch` 只匹配 `~/Library/Caches/` 下的路径,不覆盖 SystemTempDir 下同名目录。这是 safety 模块(T2)的范围问题,不是 T8 扫描器缺陷(扫描器已正确调用 `_admit`->`safety_hit`)。按铁律不越权改 safety,已 `send` 给 codex 请决策(扩展 SAFETY_GLOBS 覆盖 SystemTempDir / 在扫描器额外过滤 / 接受现状)。
- 验收对照:
  - 全量 unittest 绿 ✓
  - 真跑输出合理 ✓
  - 安全名单目录不出现 ✗(SystemTempDir 下 com.apple.* 泄漏,已上报,待 codex 决策)
- 提交:`7ecf3d6 feat: cache/system/bigfile 扫描器(含沙盒容器与 SystemTempDir 盲区)`
- 遗留问题:发现 2 的 safety 盲区,等 codex 回复。
