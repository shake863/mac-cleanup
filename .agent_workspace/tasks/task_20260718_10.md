# task_20260718_10:T10 Bash 对比验证、退役与文档切换

- 指派给:codex
- 发起者:claude
- 类型:dev
- 创建时间:2026-07-18T22:11:25+08:00

## 任务要求
严格按 docs/superpowers/plans/2026-07-18-ai-cleaner.md 的 Task 10 执行。先跑 legacy Bash -d 与 ./clean-zd clean --dry-run --include-caution 对照,逐块核对(有意放弃清单在计划中);确认无遗漏后才 git rm legacy/installer.sh/release.yml,改写 README 与 AGENTS.md,写 dev-log。发现遗漏规则:先补 rules.json(走 rework 流程通知)再退役。依赖:T8、T9 均 done。验收:全量回归绿+三条命令真跑无 traceback。

## 验收标准
(发起者补充:如何算完成)

## 执行记录

- 2026-07-18 首轮对照：旧 Bash 在无 sudo 凭据下先停于密码提示；随后使用仅允许 `sudo -v`、`sudo -n true` 与非特权 `/usr/bin/du` 的临时 dry-run shim，估算 46.19GiB，并在 Continue 提示处中止，未进入真实清理。
- Python 首轮 `clean --dry-run --include-caution --json` 暴露 `~/.Trash` 权限异常；已按 TDD 在 `rule_targets` 枚举边界增加 `OSError` 容错，新增回归测试。修复后全量 59 个测试通过，Python dry-run 为 479 项/42.0GB、0 errors。
- 逐块核对发现 T5 真遗漏：`~/Library/Application Support/minecraft/launcher_cef_log.txt` 未被当前 Minecraft 规则覆盖。已按约定将 T5 置 rework 并通知 trae；T10 在补齐前暂停，旧 Bash、installer 和 release workflow 均未删除。
- T5 rework review：提交 `88094ff` 已补 `launcher_cef_log.txt` 并增加规则数据回归测试；专项 4 个、当时全量 60 个测试通过，review 通过后恢复 T10。
- 覆盖结论：旧 Bash 各清理块均已由 path/command rules 覆盖，或列入计划的用户态有意放弃清单；未发现第二个真实规则遗漏。
- 退役：删除 `legacy/clean-zd.bash`、`installer.sh`、`.github/workflows/release.yml`；重写 `README.md` 与 `AGENTS.md`；新增 `docs/dev-log/2026-07-18-v2-python-rewrite.md`。
- 退役后首轮全类别 scan 发现 `~/Desktop` TCC `PermissionError`；已按 TDD 为 bigfile 根目录枚举补 `OSError` 容错和回归测试。
- 最终验证：全量 61 个测试通过；`./clean-zd status`、`./clean-zd scan`、`./clean-zd clean --dry-run` 和 include-caution JSON dry-run 均真跑无 traceback。全类别 scan 317 行；include-caution 为 479 项/约 42.0GB、0 errors。
