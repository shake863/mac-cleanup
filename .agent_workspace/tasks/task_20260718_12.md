# task_20260718_12:修复 SystemTempDir 系统关键缓存 safety 盲区

- 指派给:codex
- 发起者:codex
- 类型:bugfix
- 创建时间:2026-07-18T23:25:25+08:00

## 任务要求
扩展统一 safety 防线，覆盖 SystemTempDir 下与 docs/reference/lemon-cleaner-knowledge.md §1.1 同名的 Dock、FontRegistry、Spotlight 等系统关键缓存；必须 TDD，补 dev-log，复跑扫描与全量测试。

## 验收标准

- SystemTempDir 下的 Dock、FontRegistry、Spotlight 同名关键缓存命中 `safety_hit`。
- 普通 SystemTempDir 缓存不被误伤。
- 全量测试通过；真实 cache scan 不再输出上述关键名称候选。

## 执行记录

- 根因：原 `SAFETY_GLOBS` 将 §1.1 关键名称绑定到 `~/Library/Caches`，SystemTempDir 换位置后无法命中。
- 修复：新增同一批 §1.1 名称的路径组件 glob，由统一 `safety_hit` 防线匹配；未在扫描器复制过滤逻辑。
- TDD：新增测试先稳定复现三个关键名称均返回 `None`，再实施修复使测试转绿；普通腾讯缓存对照保持不命中。
- 改动文件：`cleanzd/safety.py`、`tests/test_safety.py`、`docs/reference/lemon-cleaner-knowledge.md`、`docs/dev-log/2026-07-18-system-temp-safety.md`。
- 验证：safety 专项 11 个测试、全量 50 个测试通过；真实 cache scan 共 154 个候选，关键名称命中 0。
