# task_20260718_05:T5 rules.json 规则库数据

- 指派给:trae
- 发起者:claude
- 类型:dev
- 创建时间:2026-07-18T22:11:25+08:00

## 任务要求
严格按 docs/superpowers/plans/2026-07-18-ai-cleaner.md 的 Task 5 执行:rules.json 完整内容已在计划中给出,原样落盘,不得增删改条目(它是 legacy Bash 转译+lemon 知识的定稿)。依赖:T4 已 done。验收:tests.test_rules_data 3 个测试全绿。

## 验收标准
(发起者补充:如何算完成)

## 执行记录

- 执行者:trae
- 完成时间:2026-07-18(UTC+8)
- 改动文件:
  - 新增 `cleanzd/rules.json`--32 条 path_rules + 11 条 command_rules + 2 条 aliases,原样落盘计划文档 Task 5 Step 3 给出的完整 JSON,未增删改任何条目。
  - 新增 `tests/test_rules_data.py`--3 个测试(加载与 id 唯一性、所有 path 在 `~`/`$` 内、risk 取值合法)。
- 校验结果:`load_rules()` 实跑:path_rules=32(≥25)、command_rules=11(≥10)、aliases=2、id 全局唯一(True)。
- 测试:
  - `python3 -m unittest tests.test_rules_data -v` -> 3/3 PASS。
  - `python3 -m unittest discover -s tests` -> 35/35 PASS(此前 32,新增 3)。
- 提交:`cf23589 feat: rules.json 规则库--Bash 清理块转译 + lemon 风险标记`
- 遗留问题:无。

## Review 返工记录

- 审查者：codex
- 发现阶段：T10 新旧 Bash dry-run 逐块对照
- 结论：驳回返工。旧 Bash 明确收集 `~/Library/Application Support/minecraft/launcher_cef_log.txt`，现有 `minecraft-dirs` 未列该文件，`minecraft-logs` 的 `match: "*.log"` 也不会匹配 `.txt`，属于真实转译遗漏。
- 返工要求：在 Minecraft 规则中补齐 `launcher_cef_log.txt`，并增加 `tests/test_rules_data.py` 回归断言证明该路径存在于规则库；专项与全量测试通过后重新提交并通知 codex。
