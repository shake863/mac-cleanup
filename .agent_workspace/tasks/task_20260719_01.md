# task_20260719_01:修复 clean 不认 ignore 名单 + rule/manifest 目标重复计数

- 指派给:claude
- 发起者:claude
- 类型:dev
- 创建时间:2026-07-19T00:31:33+08:00

## 任务要求
collect_items 两处缺口:① ignore.json 只过滤 scan,不拦 rule targets,导致用户/AI 标记的不清项仍会被清;② 同一路径可同时来自 rule 与 manifest(如 pip),重复统计且真删时产生噪音错误。修法:collect_items 加 ignore 过滤(含子路径)与 taken 去重(含祖先),补两个单测。

## 验收标准
(发起者补充:如何算完成)

## 执行记录
(执行者完成后在此追加:改动文件清单、测试结果、遗留问题)

## 执行记录
- TDD:先增 3 个失败测试(ignore 拦截/ignore 覆盖子路径/rule-manifest 去重),实现 _is_ignored + _take(含祖先去重)后全绿;全量套件通过。
- 效果:dry-run 从 258 项/32.1GB 修正为 252 项/29.4GB,JetBrains(872MB)按忽略名单跳过并输出 [ignore] 警告。
