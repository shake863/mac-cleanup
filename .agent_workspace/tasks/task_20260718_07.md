# task_20260718_07:T7 scan 编排/渲染与 manifest 子命令接线

- 指派给:trae
- 发起者:claude
- 类型:dev
- 创建时间:2026-07-18T22:11:25+08:00

## 任务要求
严格按 docs/superpowers/plans/2026-07-18-ai-cleaner.md 的 Task 7 执行。Candidate 数据类、decided/safety/rule_covered 三重过滤、表格与 JSON 渲染、manifest add/remove/ignore/list CLI 接线、五个扫描器占位模块。依赖:T6 已 done。验收:全量 unittest 绿;./clean-zd scan 输出「没有新的候选清理项」;./clean-zd manifest list 正常。

## 验收标准
(发起者补充:如何算完成)

## 执行记录

- 执行者:trae
- 完成时间:2026-07-18(UTC+8)
- 改动文件:
  - 新增 `cleanzd/scan/__init__.py`--`Candidate` dataclass、`rule_covered`(path 规则 base 展开集合)、`_admit`(decided∪safety 过滤,去重)、`run_scan`(按 category 调各扫描器->过滤->按体积降序)、`render_json`/`render_table`。
  - 新增 5 个占位扫描器:`cleanzd/scan/{cache,dev,leftover,systemitems}.py`(`scan() -> []`)、`cleanzd/scan/bigfile.py`(`scan(threshold_mb, dirs) -> []`),Task 8/9 填充。
  - 修改 `cleanzd/__main__.py`--在 `if args.cmd == "status"` 前插入 scan 与 manifest(add/remove/ignore/list)子命令分支;manifest list 的 JSON 输出适配 codex T6 版本的 `json_module` 变量名(替代计划文档的 `_json`,行为不变)。
- 代码来源:原样落地计划文档 Task 7 Step 3;唯一偏离为上述 `json_module` 命名适配(因 codex T6 已将该变量改名,保持文件内一致)。
- 测试:
  - `python3 -m unittest tests.test_scan -v` -> 4/4 PASS(AdmitTest / RenderTest×2 / RunScanTest)。
  - `python3 -m unittest discover -s tests` -> 46/46 PASS(此前 42,新增 4)。
- 手工验证:
  - `./clean-zd scan` -> 输出「没有新的候选清理项」,退出码 0。
  - `./clean-zd manifest list`(临时 `CLEAN_ZD_CONFIG_DIR`)-> 退出码 0,空清单/忽略名单正常输出。
- 提交:`b0fcdf4 feat: scan 编排/渲染/过滤与 manifest 子命令接线`
- 遗留问题:无。占位扫描器待 Task 8(cache/system/bigfile)与 Task 9(dev/leftover)填充。
